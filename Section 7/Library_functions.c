/// Library Functions in Cprogramming
#include<stdio.h>
#include<math.h>
intmain(){
    double n,s;
    printf("Enter a number:");
    scanf('%lf',&n);
    s=sqrt(n);
    printf("The square root of %lf is %lf",n,s);
    return 0;
}